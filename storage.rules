rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // DEVELOPMENT MODE: Allow any logged-in user to upload/download files
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
